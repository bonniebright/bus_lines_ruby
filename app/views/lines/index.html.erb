<h2> Disney Bus System </h2>

<ul>
  <% @lines.each do |line| %>

<li>
    <%= link_to line.color, line_path(line), :class => "big" %>
</li>
  <% end %>
</ul>

<h2> Disney Bus Stations: </h2>
<ul>
  <% @stations.each do |station| %>
  <li> <%= link_to station.name, station_path(station), :class => "big" %>
  </li>
    <% end %>
</ul>


